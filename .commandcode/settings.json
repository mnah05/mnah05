{
  "permissions": {
    "allow": [
      "Shell(git:*)",
      "Shell(npm:*)",
      "Shell(/Applications/Brave Browser.app/Contents/MacOS/Brave Browser:*)",
      "Shell(echo:*)",
      "Shell(cat > /tmp/cdp-check.mjs <<'EOF' const http = require(\"http\"); function getJson(path) { return new Promise((res, rej) => { http.get({ host: \"127.0.0.1\", port: 9222, path }, (r) => { let d = \"\"; r.on(\"data\", (c) => (d += c)); r.on(\"end\", () => res(JSON.parse(d))); }).on(\"error\", rej); }); } const pages = await getJson(\"/json/list\"); const page = pages.find((p) => p.type === \"page\"); const ws = new WebSocket(page.webSocketDebuggerUrl); const id = (m) => new Promise((res, rej) => { const cb = (ev) => { if (ev.id === m.id) { ws.removeEventListener(\"message\", cb); res(ev.result); } }; ws.addEventListener(\"message\", (ev) => cb(JSON.parse(ev.data))); }); let n = 1; const send = (method, params = {}) => { const m = { id: n++, method, params }; ws.send(JSON.stringify(m)); return id(m); }; await new Promise((r) => (ws.onopen = r)); await send(\"Emulation.setDeviceMetricsOverride\", { width: 375, height: 500, deviceScaleFactor: 1, mobile: false }); await send(\"Page.navigate\", { url: \"http://localhost:5199/\" }); await new Promise((r) => setTimeout(r, 1500)); const res = await send(\"Runtime.evaluate\", { expression: `JSON.stringify({ innerWidth: window.innerWidth, docScrollWidth: document.documentElement.scrollWidth, headerNav: (() => { const nav = document.querySelector('header nav'); const r = nav.getBoundingClientRect(); return { left: r.left, right: r.right, width: r.width }; })(), brand: (() => { const e = document.querySelector('.brand'); const r = e.getBoundingClientRect(); return { left: r.left, right: r.right, width: r.width }; })(), navLinks: (() => { const e = document.querySelector('.nav-links'); const r = e.getBoundingClientRect(); return { left: r.left, right: r.right, width: r.width }; })(), homeLink: (() => { const e = document.querySelector('.nav-links a'); const r = e.getBoundingClientRect(); return { left: r.left, right: r.right }; })() })`, returnByValue: true, }); console.log(res.result.value); ws.close(); EOF node /tmp/cdp-check.mjs)",
      "Shell(mv:*)",
      "Shell(node cdp-check.cjs)",
      "Shell(cat > /tmp/cdp-check.mjs <<'EOF' const http = require(\"node:http\"); function getJson(path) { return new Promise((res, rej) => { http.get({ host: \"127.0.0.1\", port: 9222, path }, (r) => { let d = \"\"; r.on(\"data\", (c) => (d += c)); r.on(\"end\", () => res(JSON.parse(d))); }).on(\"error\", rej); }); } async function main() { const pages = await getJson(\"/json/list\"); const page = pages.find((p) => p.type === \"page\"); const ws = new WebSocket(page.webSocketDebuggerUrl); const pending = new Map(); let n = 1; ws.addEventListener(\"message\", (ev) => { const m = JSON.parse(ev.data); if (pending.has(m.id)) { pending.get(m.id)(m.result); pending.delete(m.id); } }); const send = (method, params = {}) => new Promise((res) => { pending.set(n, res); ws.send(JSON.stringify({ id: n++, method, params })); }); await new Promise((r) => (ws.onopen = r)); await send(\"Emulation.setDeviceMetricsOverride\", { width: 375, height: 500, deviceScaleFactor: 1, mobile: false }); await send(\"Page.navigate\", { url: \"http://localhost:5199/\" }); await new Promise((r) => setTimeout(r, 1500)); const res = await send(\"Runtime.evaluate\", { expression: `JSON.stringify({ innerWidth: window.innerWidth, docScrollWidth: document.documentElement.scrollWidth, headerNav: (() => { const r = document.querySelector('header nav').getBoundingClientRect(); return { left: r.left, right: r.right, width: r.width }; })(), brand: (() => { const r = document.querySelector('.brand').getBoundingClientRect(); return { left: r.left, right: r.right, width: r.width }; })(), navLinks: (() => { const r = document.querySelector('.nav-links').getBoundingClientRect(); return { left: r.left, right: r.right, width: r.width }; })(), homeLink: (() => { const r = document.querySelector('.nav-links a').getBoundingClientRect(); return { left: r.left, right: r.right }; })() })`, returnByValue: true, }); console.log(res.result.value); ws.close(); } main(); EOF node /tmp/cdp-check.mjs)",
      "Shell(sed:*)",
      "Shell(node cdp-check.mjs)",
      "Shell(cat > /tmp/cdp-check-320.mjs <<'EOF' import http from \"node:http\"; function getJson(path) { return new Promise((res, rej) => { http.get({ host: \"127.0.0.1\", port: 9222, path }, (r) => { let d = \"\"; r.on(\"data\", (c) => (d += c)); r.on(\"end\", () => res(JSON.parse(d))); }).on(\"error\", rej); }); } async function main() { const pages = await getJson(\"/json/list\"); const page = pages.find((p) => p.type === \"page\"); const ws = new WebSocket(page.webSocketDebuggerUrl); const pending = new Map(); let n = 1; ws.addEventListener(\"message\", (ev) => { const m = JSON.parse(ev.data); if (pending.has(m.id)) { pending.get(m.id)(m.result); pending.delete(m.id); } }); const send = (method, params = {}) => new Promise((res) => { pending.set(n, res); ws.send(JSON.stringify({ id: n++, method, params })); }); await new Promise((r) => (ws.onopen = r)); await send(\"Emulation.setDeviceMetricsOverride\", { width: 320, height: 500, deviceScaleFactor: 1, mobile: false }); await send(\"Page.navigate\", { url: \"http://localhost:5199/about\" }); await new Promise((r) => setTimeout(r, 1500)); const res = await send(\"Runtime.evaluate\", { expression: `JSON.stringify({ innerWidth: window.innerWidth, docScrollWidth: document.documentElement.scrollWidth, navLinks: (() => { const r = document.querySelector('.nav-links').getBoundingClientRect(); return { left: r.left, right: r.right }; })(), aboutLink: (() => { const r = document.querySelector('.nav-links a:last-child').getBoundingClientRect(); return { left: r.left, right: r.right }; })() })`, returnByValue: true, }); console.log(res.result.value); ws.close(); } main(); EOF node /tmp/cdp-check-320.mjs)",
      "Shell(kill $(cat /tmp/brave.pid) 2>/dev/null; rm -f /tmp/brave.pid /tmp/cdp-check.mjs /tmp/cdp-check-320.mjs /tmp/home-1440.png /tmp/home-mobile.png /tmp/about-1440.png /tmp/mobile-top.png /tmp/home-mobile-header.png; echo cleaned)",
      "Shell(git commit -F - <<'EOF' redesign site: warm minimal theme, hide placeholder blog Co-authored-by: CommandCodeBot <noreply@commandcode.ai> EOF)",
      "Shell(git commit -F - <<'EOF' redesign site: warm minimal theme, hide placeholder blog Co-authored-by: CommandCodeBot <noreply@commandcode.ai> EOF git push origin main && git status --short)",
      "Shell(cd /Users/mnah/code-space/domain-page && git add src/components/Footer.tsx && git commit -F - <<'EOF' add email contact link to footer Co-authored-by: CommandCodeBot <noreply@commandcode.ai> EOF)",
      "Shell(npx wrangler deploy)"
    ],
    "deny": [],
    "defaultMode": "default"
  }
}
